[tools]
gitleaks = "latest"
lychee = "latest"
rumdl = "latest"

[tasks.pre-commit]
description = "Run pre-commit checks"
run = """
gitleaks protect -v --staged
lychee .
rumdl check
"""

[tasks.format-md]
description = "Format markdown files"
run = "rumdl check --fix"
