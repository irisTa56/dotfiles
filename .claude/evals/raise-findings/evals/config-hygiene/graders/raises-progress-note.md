---
type: llm
---

The change adds a config comment that says to keep the ignores "until phase 2 of the sync rollout lands", a progress note in shipped config.

PASS if the reply raises that comment as a finding, as a progress or process note that does not belong in the config.

FAIL if the reply does not raise the comment.
